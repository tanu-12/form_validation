let id = (id) => document.getElementById('id');
let classes = (classes) => document.getElementsByClassName('classes');
let username = id('username');
let email = id('email');
let password = id('password');
let form = id('form');
let errorMs = classes('error');

